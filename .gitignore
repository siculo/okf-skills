# OS
.DS_Store
Thumbs.db
desktop.ini

# Editors
.vscode/
.idea/
*.swp
*.swo
*~

# Claude Code local settings (keep commands, ignore local overrides)
.claude/settings.local.json

# OKF invocation log (contains absolute local paths — not portable)
.claude/invocation-log.jsonl
